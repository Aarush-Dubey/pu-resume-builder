# PU Resume Builder

vibe coded this shit because editing in google doc is annoying af

## start

download this thing.

then either:

```bash
python3 -m http.server 8000
```

open:

```text
http://localhost:8000
```

or just double click `index.html` if browser behaves.

fill form. edit preview if needed. print/pdf. done.
